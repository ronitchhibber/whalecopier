# =============================================================================
# POLYMARKET WHALE COPY TRADING - PRODUCTION DEPENDENCIES
# Version: 1.0.0 | Python: 3.9+
# =============================================================================

# Polymarket API & Blockchain
py-clob-client==0.20.0          # Updated to latest version
web3==6.11.0
eth-account==0.10.0
eth-keys==0.4.0

# Web Framework & API (NEW - CRITICAL FOR PRODUCTION)
fastapi==0.104.0                # High-performance async API framework
uvicorn[standard]==0.24.0       # ASGI server with WebSocket support
python-multipart==0.0.6         # Form data parsing
starlette==0.27.0               # ASGI framework (FastAPI dependency)

# Data manipulation & analysis
pandas==2.1.0
numpy==1.26.0
scipy==1.11.0
scikit-learn==1.3.0
statsmodels==0.14.0

# Financial metrics
empyrical==0.5.5
quantstats==0.0.62

# Database
psycopg2-binary==2.9.9
sqlalchemy==2.0.23
alembic==1.12.1
asyncpg==0.29.0                 # Async PostgreSQL driver (NEW)

# Async & networking
asyncio-mqtt==0.16.1
aiohttp==3.9.0
websockets==12.0
httpx==0.25.0
aiofiles==23.2.0                # Async file operations (NEW)

# Message queues
kafka-python==2.0.2
confluent-kafka==2.3.0
pika==1.3.2
aiokafka==0.10.0                # Async Kafka (NEW)
aio-pika==9.3.0                 # Async RabbitMQ (NEW)
celery==5.3.0                   # Distributed task queue (NEW)

# Redis
redis==5.0.1
hiredis==2.2.3

# Configuration
python-dotenv==1.0.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Security & Cryptography (NEW - CRITICAL)
cryptography==41.0.0
pyjwt==2.8.0
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
boto3==1.29.0                   # AWS SDK (KMS, Secrets Manager)
botocore==1.32.0

# Monitoring & logging
prometheus-client==0.19.0
sentry-sdk[fastapi]==1.38.0     # FastAPI integration
structlog==23.2.0
colorlog==6.8.0
python-json-logger==2.0.7       # JSON structured logging (NEW)
loguru==0.7.0                   # Advanced logging (NEW)

# OpenTelemetry (NEW - for distributed tracing)
opentelemetry-api==1.21.0
opentelemetry-sdk==1.21.0
opentelemetry-instrumentation-fastapi==0.42b0

# Utilities
python-dateutil==2.8.2
pytz==2023.3
tqdm==4.66.1
retry==0.9.2
tenacity==8.2.3
click==8.1.7                    # CLI framework (NEW)
rich==13.7.0                    # Rich terminal output (NEW)
tabulate==0.9.0                 # Table formatting (NEW)

# Rate Limiting & Caching (NEW)
slowapi==0.1.9                  # Rate limiting for FastAPI
cachetools==5.3.2
diskcache==5.6.3

# Testing & development
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
pytest-mock==3.12.0
pytest-xdist==3.5.0             # Parallel testing (NEW)
httpx-mock==0.15.0              # HTTP mocking (NEW)
faker==20.0.0                   # Fake data (NEW)
factory-boy==3.3.0              # Test fixtures (NEW)

# Code Quality
black==23.11.0
flake8==6.1.0
mypy==1.7.0
isort==5.12.0                   # Import sorting (NEW)
pylint==3.0.0                   # Code analysis (NEW)
bandit==1.7.5                   # Security linting (NEW)
pre-commit==3.5.0               # Git hooks (NEW)

# Development Tools
ipython==8.18.0
jupyter==1.0.0
ipdb==0.13.13                   # IPython debugger (NEW)
watchdog==3.0.0                 # File monitoring (NEW)

# Technical indicators
ta-lib==0.4.28

# System utilities (NEW)
psutil==5.9.6                   # System monitoring
gunicorn==21.2.0                # Production WSGI server

# WebSocket & Real-time (NEW)
python-socketio==5.10.0
websocket-client==1.6.4

# Data Validation (NEW)
marshmallow==3.20.0
jsonschema==4.20.0
msgpack==1.0.7

# Time utilities (NEW)
arrow==1.3.0
pendulum==2.1.2

# =============================================================================
# INSTALLATION INSTRUCTIONS:
#
# 1. Install all dependencies:
#    pip install -r requirements.txt
#
# 2. For TA-Lib (optional):
#    macOS: brew install ta-lib
#    Linux: sudo apt-get install libta-lib-dev
#    Then: pip install ta-lib
#
# 3. System requirements:
#    - PostgreSQL 14+ with TimescaleDB
#    - Redis 7+
#    - Kafka 3.0+ (optional)
#    - RabbitMQ 3.12+ (optional)
#
# 4. AWS CLI (for KMS):
#    pip install awscli
#    aws configure
# =============================================================================
